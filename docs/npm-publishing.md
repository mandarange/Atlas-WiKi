# npm Publishing

The package name is `@mandarange/atlas-wiki`; publishing is public and provenance-enabled through `publishConfig`. Release requires dry-run, tarball install smoke, ESM import smoke, CLI bin smoke, declarations, and export allowlist verification.
