# Roadmap

## v0.1

TypeScript package, SQLite store, ingest, ACL search, context packs, CLI, SDK, MCP, validation, backup, export/import foundations.

## v0.2

Connectors, approval workflow hooks, richer conflict/freshness scoring, HTTP deployment.

## v1.0

Federated sync, hybrid retrieval, tamper detection, enterprise policy workflows, larger eval fixtures.
