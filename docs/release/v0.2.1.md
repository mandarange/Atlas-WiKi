# Atlas WiKi v0.2.1

v0.2.1 is the final 9+ blocker-closure candidate for the v0.2.0 RC line. It is not a completed npm/GitHub release until the tag, GitHub Release, npm package, published smoke, and postpublish evidence exist for `v0.2.1`.

## Fixed

- Replace Supabase `validate()` and `migrationReport()` stubs with read-only validation and migration status evidence.
- Reload Supabase custom schema contracts from persisted `schema_contract` records across store instances.
- Bind release evidence to the F9 checklist, CI run URL, scorecard evidence paths, RAG eval metrics, Supabase local smoke output, and package smoke output.
- Keep README structured extraction examples aligned with registered custom schema usage.

## Gates

- `npm run test:supabase:mock`
- `npm run test:structured`
- `npm run rag:eval`
- `npm run release:next-stable-verify`
- `npm run release:check`
- After publish only: `ATLAS_WIKI_PUBLISHED_SPEC=atlas-wiki@0.2.1 npm run release:published-check`
