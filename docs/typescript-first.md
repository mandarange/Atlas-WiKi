# TypeScript First

The source of truth is `src/**/*.ts`. Runtime JavaScript and declarations are generated into `dist/` by `tsc`. NodeNext, ESM, declaration emit, explicit exports, and no hand-authored `src/**/*.js` are required.
